import { NgbDropdownDirective } from './ngb-dropdown.directive';

describe('NgbDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new NgbDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
