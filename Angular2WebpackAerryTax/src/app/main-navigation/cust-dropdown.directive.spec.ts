import { CustDropdownDirective } from './cust-dropdown.directive';

describe('CustDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new CustDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
